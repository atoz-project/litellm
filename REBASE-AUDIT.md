# REBASE-AUDIT — custom-aigw → upstream/main (2026-09-06)

- Fork tip: `custom-aigw` = `f606cafa4c` (do not move)
- Fork base (merge-base with upstream/main): `bc6e7df05b` = tag `v1.98.0-rc.1`
- Rebase target: `upstream/main` = `7d5b6456ba`
- Fork-local commits: 24 non-merge + 4 merges (`git log bc6e7df05b..custom-aigw`)
- Fork diff vs base: 32 files, +3327/-70 (`git diff --stat bc6e7df05b..custom-aigw`)

Legend: **SOLVED** = upstream already handles the root cause → drop patch.
**PARTIAL** = upstream has adjacent machinery but not the root cause → modified port.
**NOT-SOLVED** = re-apply logic onto upstream structures.

---

## Audit table

| # | Patch (fork commits) | Intent / 根因 | Upstream status (evidence @ upstream/main) | Action for rebase |
|---|---|---|---|---|
| 1 | 428 cooldown (`1ce7d2c78a`) | Cool down 428 Precondition Required (Kimi plan-key wall), upstream only cooled 401/404/408/429 | **NOT-SOLVED** — upstream `_is_cooldown_required` still only cools 429/401/408/404 among 4xx (`litellm/router_utils/cooldown_handlers.py:234-243`) | Port: add `exception_status == 428 → True` branch |
| 2 | `/reset` + `/circuit/reset` endpoints (`ae0713cbbf`) | Operator endpoint to clear cooldown + failed-call counters across replicas via DualCache (Redis-safe, no flushall) | **NOT-SOLVED** — no `/reset` route in upstream `proxy_server.py` (grep for `"/reset"`, `ResetCircuitBreakerResponse` empty); `CooldownCache` API unchanged (`cooldown_cache.py:71/107`) | Port: endpoints + `ResetCircuitBreakerResponse` in `_types.py`. Update comment "custom-aigw fork" → rebase branch |
| 3 | GLM tool_use sanitizer (`39f8d18fee`, docs `4895e26db1`) | Normalize empty `tool_use.input` → `{'_': ''}` on /v1/messages for `round-robin/glm-*` (APIG Lua parity) | **NOT-SOLVED** upstream — no equivalent sanitizer (upstream has tool-*name*/tool_use-*id* sanitizers only: `anthropic/chat/transformation.py:1057`, `anthropic/common_utils.py:1221`). Fork copy is a **root-level `glm_sanitizer.py`, NOT the live class** — runtime loads ops `litellm-gateway/glm_sanitizer.py` via `startup_patch.py:31`; fork file is a provenance snapshot (diff vs ops copy = comments/helpers only) | Drop from rebase patch set (ops repo stays source of truth). Optionally keep the root-level snapshot file |
| 4 | websearch `enabled_models` gate + boundary-prefix match + bare-name fix (`6d2860e854`, `0d90f68692`, `7a95096998`) | Per-model whitelist gating ALL interception paths (tool conversion, short-circuit, all 3 agentic-loop entries); `-` boundary so `glm-5.2` ≠ `glm-5.20`; reconstruct `<provider>/<model>` when surface sees prefix-stripped name | **NOT-SOLVED** — upstream has `enabled_providers` gate only; no `enabled_models` in `WebSearchInterceptionLogger.__init__`, `from_config_yaml`, or `WebSearchInterceptionConfig` (`integrations/websearch_interception/handler.py:211`, `types/integrations/websearch_interception.py`) | Port: `_is_model_enabled` + gates on `try_short_circuit_search`, `async_pre_call_deployment_hook`, `async_pre_request_hook`, `async_should_run_agentic_loop`, `async_should_run_chat_completion_agentic_loop`, `async_should_run_responses_agentic_loop`, `from_config_yaml` + types |
| 5 | websearch native-block short-circuit (`0518d2549a`, `60bcf52f59`, `92983459f6`) | Claude Code standalone web_search request must get native `server_tool_use`+`web_search_tool_result` blocks; deployment hook converts native tool BEFORE short-circuit sees it → `native_tool` was None → text-only response → Claude Code WebSearchTool parse failure | **SOLVED** — upstream sets `WEBSEARCH_EMIT_NATIVE_BLOCKS_KEY` in `async_pre_call_deployment_hook` (`handler.py:435`) AND `async_pre_request_hook` (`handler.py:610`), and `try_short_circuit_search` reads `native_tool` via `is_anthropic_native_web_search_tool` on the tools it receives (fork's emit_native_blocks param + original_tools save are upstream's flag approach). Verify flag actually reaches short-circuit in live test (see §Verification) | Drop fork version; keep fork's regression tests adapted to upstream mechanism |
| 6 | Aliyun IQS search provider (`e6acc87f55`) | New `SearchProviders.ALIYUN_IQS` + `litellm/llms/aliyun_iqs/search/transformation.py` (UnifiedSearch) | **NOT-SOLVED** — upstream `SearchProviders` enum ends at `NIMBLE` (`types/utils.py:3936-3960`), no `aliyun_iqs` module (`ProviderConfigManager` has no IQS entry) | Port as-is: new files + enum value + `ProviderConfigManager` registration |
| 7 | Kimi quota sync + reset-aware cooldown (ADR-0004) (`f353e5ab47`) | Probe registry (`api.kimi.com`) → 5min loop GET `{api_base}/v1/usages` → cool with authoritative `resetTime`+15min buffer / unblock on recovery; wall-hit 429 "usage limit for this period" & 403 billing-cycle → 1h blind placeholder TTL + fire-and-forget probe | **NOT-SOLVED** — no `quota_sync.py` in upstream `router_utils/`, no `run_quota_sync_loop` in `proxy_server.py` startup | Port as-is: `quota_sync.py` unchanged + startup task in `proxy_startup_event` |
| 8 | periodic-quota 403 → immediate long cooldown (`6eaf066608`, `737c29109b`) | Upstream hard-excludes 403 from cooldown AND anthropic mapper had no 403 branch → Kimi weekly-quota 403 became APIConnectionError(500) on veto list → key re-picked forever. Marker text match ("usage limit for this billing cycle"/"this period"), status-agnostic, bypasses fail-count policy | **PARTIAL** — upstream mapper NOW maps 403 → `PermissionDeniedError` (`exception_mapping_utils.py:501`, 403 branch added) so status 403 survives; but upstream `_is_cooldown_required` still returns False for all 403 (`cooldown_handlers.py:234-243`) and the APIConnectionError veto at `:222-225` still swallows mapper-fallback cases (new-api/OpenAI-compat surfaces still produce status-less errors with embedded marker text) | Modified port: keep marker matcher + veto-override + 403-with-marker branch + `_set_cooldown_deployments` bypass + `sync_deployment_quota` create_task. 403 branch is now load-bearing (mapper preserves status); APIConnectionError override still needed for OpenAI-compatible surfaces |
| 9 | Anthropic thinking→responses gating, DashScope K3 translation, routing (`540a53692d`) | (a) thinking requests bypassed `use_chat_completions_url_for_anthropic_messages` → DashScope kimi-k3 hit `/compatible-mode/v1/responses` → 400; (b) K3 thinking must become `extra_body.reasoning_effort="max"` + strip DashScope-unsupported keys; (c) per-deployment `supported_endpoints`/`mode:"responses"` must beat the global flag | **PARTIAL** — upstream `messages/handler.py:70-86` `_should_route_to_responses_api` checks global flag FIRST (flag blanket-wins, no per-deployment override), then provider∈{openai} blanket-True (no api_base check), then prefix-strip mode check. `_deployment_passes_through_anthropic_messages` (`:88-99`) reads `supported_endpoints` for "/v1/messages" passthrough but NOT for the responses/chat decision. Adapters `_route_openai_thinking_to_responses_api_if_needed` (`adapters/handler.py:321-383`) still ignores the global flag and model_info. NO DashScope/K3 translation anywhere upstream | Modified port: (a)+(b) port fork logic into upstream structure; (c) NEW semantics per rebase spec: `_should_route_to_responses_api` = per-deployment declaration (`supported_endpoints`∋"/v1/responses", or `mode:"responses"`) wins first; global flag = fallback default for undeclared deployments; non-openai provider → chat; openai provider → chat unless api_base is api.openai.com. Same precedence applied to the thinking path in adapters |
| 10 | reasoning_effort normalize + minimal/xhigh/max capability plumbing | Claude Code adaptive `output_config.effort` must pass through unchanged for whitelisted models, degrade to nearest supported otherwise (`max→xhigh`, `minimal→low`) | **SOLVED** — upstream `adapters/handler.py` `_normalize_reasoning_effort` (called in `_prepare_completion_kwargs` before `_route_openai_thinking`) → `experimental_pass_through/utils.py:37` `normalize_reasoning_effort_value` → `router_utils/reasoning_effort_capability.py` (supports_{none,minimal,low,xhigh,max}_reasoning_effort flags + `reasoning_effort_levels` declared sets). Ops `startup_patch.py` registers capabilities via `litellm.register_model` — compatible (flag keys identical) | Drop fork patch (fork had none — already base). Verify: ops `test_effort_max_passthrough.py` must pass |
| 11 | cost-map registration key normalization (`e1d831b6ae`) | `register_model` wrote `openai/openai/kimi-k3` when model already provider-prefixed → deployment mode/cache-pricing unreadable at lookup; UUID deployment-id keys spammed "not in built-in cost map" warnings (87/boot) | **PARTIAL** — warning half SOLVED (`warning_display_name` param in `register_model`, `utils.py:3019`; main.py passes `shared_key`, router passes `model`). Key-normalization half NOT-SOLVED: `main.py:1220` `shared_key = f"{custom_llm_provider}/{model}"` and `router.py:9703` `_backend_cost_map_keys` still unconditionally prefix | Modified port: add `provider_qualified_cost_map_key` helper to `utils.py`; use it at `main.py:1220` and `router.py:9703`; drop fork's `warn_on_missing_builtin_entry` param (upstream's `warning_display_name` is the better mechanism) |
| 12 | pass-through logging payload for unpriced models (`01a9e247e0`) | `completion_cost` raises "isn't mapped yet" for unpriced models; one big try/except dropped response_cost+model+provider + full traceback per request (Kimi k3 subscription = real cost 0) | **NOT-SOLVED** — upstream `anthropic_passthrough_logging_handler.py:284` still calls `litellm.completion_cost(...)` inside the same monolithic try | Port: `_response_cost_for_unpriced_safe_model` (isolate price calc; unpriced → 0.0, warn once per model; other errors still loud) |
| 13 | GET `/config/get` + DELETE `/config/param` (`6f095da034`, `f606cafa4c`) | Terraform `litellm_config` drift detection (distinguish "DB override row" vs "config.yaml baseline") + Delete restores yaml; function named `read_config_param` to avoid shadowing imported `get_config_param` helper | **NOT-SOLVED** — no such endpoints upstream (grep empty) | Port as-is (endpoints in proxy_server.py CONFIG MANAGEMENT section, reuse `ConfigRepository.get_param/delete_param`, `invalidate_config_param`) |
| 14 | `/router/settings` 500 traceback logging (`0f713fd2e3`) | `.error` → `.exception` so stderr traceback reaches SAE SLS | **NOT-SOLVED** — upstream `router_settings_endpoints.py:123,171` still `.error` | Port: 2-line change |
| 15 | periodic-quota immediate long-cooldown blind TTL | (folded into #7/#8 — 1h `QUOTA_BLIND_COOLDOWN_SECONDS` placeholder replaces fork's original fixed 6h TTL per ADR-0004) | see #7/#8 | see #7/#8 |
| 16 | 429 cooldown threshold semantics, Redis-backed cooldown | Verified: **no fork divergence** — upstream `_should_cooldown_deployment` 429 handling (`cooldown_handlers.py:386`) and Redis-backed `CooldownCache` (DualCache, `cooldown_cache.py`) are upstream baseline; memory notes "reset Redis cooldown" is an ops runbook item, not a patch | n/a | nothing to port |
| 17 | gpt-6-astra xhigh/max capability registration | Lives in ops `startup_patch.py` (`model_capabilities`), NOT in fork — explicitly excluded per task | n/a | out of scope |
| 18 | CI: ACR build workflow, amd64-only, disable OSV/image scans (`2db7084b61`, `623e2d8b17`, `8fa7af9956`) | Fork infra: build+push custom-aigw base images to ACR on branch push | n/a (fork-specific, upstream-neutral) | Port as-is (3 workflow files; inert upstream) |
| 19 | tests (`5bf627e909` + test files) | Fork regression tests for quota_sync, periodic quota cooldown, websearch enabled_models, adapters/messages handler, IQS, passthrough logging, cost-map | n/a | Port alongside their patches; adapt to upstream structures where mechanism changed (#5, #9, #11) |

---

## Port plan for `rebase-2026-09` (one commit per patch)

1. `ci`: build-and-push.yml + amd64-only + drop scheduled OSV/image scans (#18)
2. `feat(search)`: Aliyun IQS provider (#6)
3. `fix(router)`: 428 cooldown (#1)
4. `feat(proxy)`: /reset + /circuit/reset (#2)
5. `feat(router)`: quota_sync.py + startup loop (#7)
6. `fix(router)`: periodic-quota 403/marker immediate cooldown (#8)
7. `feat(websearch_interception)`: enabled_models gate (#4) + tests
8. `fix(passthrough)`: unpriced-model logging payload (#12) + test
9. `fix(cost-map)`: provider_qualified_cost_map_key (#11) + tests
10. `feat(proxy)`: /config/get + /config/param (#13)
11. `fix(proxy)`: /router/settings traceback (#14)
12. `feat(anthropic)`: per-deployment responses-routing precedence + DashScope K3 thinking translation (#9) + tests
13. `test`: fork regression tests adapted to upstream mechanisms (#5, #19)

## Verification checklist (post-port)

- [ ] `python -m compileall` on all touched files
- [ ] In-tree pytest: `tests/test_litellm/router_utils/test_quota_sync.py`, `test_periodic_quota_cooldown.py`, `test_register_model_custom_pricing.py`, `test_router_model_cost_isolation.py`, websearch enabled_models tests, anthropic adapters/messages handler tests, IQS search tests, passthrough logging test
- [ ] Ops regression: `cd ai-gateway-ops && PYTHONPATH=../litellm python3 -m pytest tests/test_effort_max_passthrough.py -q` (exercises upstream `_prepare_completion_kwargs` + `normalize_reasoning_effort_value` + ops `startup_patch` capability registrations + `QwenEffortClampLogger`)
- [ ] **Live gateway (cannot verify offline)**: (a) flag=on + per-deployment `mode:"responses"` declaration now wins (new semantics — ops config must be audited before deploying this build: any deployment that RELIED on the global flag blanket-overriding its declarations changes behavior); (b) websearch native-block short-circuit via upstream `WEBSEARCH_EMIT_NATIVE_BLOCKS_KEY`; (c) Kimi 403 now maps to PermissionDeniedError upstream — confirm marker path still triggers via the ported 403 branch; (d) quota_sync loop against real `api.kimi.com/v1/usages`

## Verification results (rebase-2026-09, 2026-09-06)

- [x] `python -m compileall` on all touched files — OK
- [x] In-tree pytest — all pass: quota_sync (26), periodic_quota_cooldown + register_model_custom_pricing (52), websearch enabled_models (23), reasoning capability/normalization (65), messages handler full suite (56), adapters full suite (190), IQS search (1), 6 unpriced-logging tests. Passthrough logging suite: 12 pre-existing upstream pricing-approximation failures reproduce identically on unmodified upstream (verified via stash) — unrelated to the port.
- [x] Ops regression `test_effort_max_passthrough.py`: **30/30 pass**. Caught one genuine upstream-refactor break: upstream's advertisement-opt-out gave off-map deployments `minimal` on the request path (deepseek minimal → minimal instead of → low). Fixed as commit `4d685a8733` (request-path gate: `minimal` only on explicit opt-in; /model_group/info advertisement untouched). The fork's websearch-native-block test was superseded — upstream implements the same mechanism via `WEBSEARCH_EMIT_NATIVE_BLOCKS_KEY` (handler.py:435/610) with its own richer suite.
- [ ] **Live gateway (cannot verify offline)**: (a) flag=on + per-deployment `mode:"responses"` declaration now wins (new semantics — audit ops config before deploying: any deployment that RELIED on the global flag blanket-overriding its declarations changes behavior); (b) websearch native-block short-circuit via upstream `WEBSEARCH_EMIT_NATIVE_BLOCKS_KEY`; (c) Kimi 403 now maps to PermissionDeniedError upstream — the ported marker+403 branch covers it; (d) quota_sync loop against real `api.kimi.com/v1/usages`.
